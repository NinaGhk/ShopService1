package order;

import jdk.internal.access.JavaSecurityAccess;
import jdk.internal.jimage.ImageStream;
import product.Product;

import java.awt.*;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.List;

public class OrderRepo {

    public Order addOrder;

    public static void main(String[]){


        public HashMap<Integer, Order> product = new HashMap<>();


        public void addOrder(Order order){
        Order.put(order.getId().order);

        public List<Order> listOfOrder(){
            ArrayList<Order> orders = new ArrayList<>(orders.values());
            return listOfOrder;
        }

        public Order getOfOrder(Integer id){
            Order foundOrder = getOfOrder.get(id);
        }

          public Order addOfOrder(){
             return addOfOrder;
        }

    }
}
